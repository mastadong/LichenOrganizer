<Window x:Class="LichenOrganizer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LichenOrganizer.UI" 
        xmlns:views="clr-namespace:LichenOrganizer.UI.Views"
        xmlns:viewmodels="clr-namespace:LichenOrganizer.UI.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewmodels:MainViewModel}"
        Title="LichenOrganizer" Height="450" Width="800" FontSize="20">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!-- NAVIGATION VIEW -->
        <views:NavigationView DataContext="{Binding NavigationViewModel}" />

        <!-- DETAIL PANEL -->
        <views:FriendDetailView Grid.Column="1" DataContext="{Binding FriendDetailViewModel}"/>

    </Grid>

</Window>